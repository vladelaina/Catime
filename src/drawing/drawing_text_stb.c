/**
 * @file drawing_text_stb.c
 * @brief Translation-unit anchor for the split STB text renderer.
 *
 * Public entry points are grouped in focused companion modules.  Keeping
 * this stable anchor preserves the original source path for build tooling.
 */

#include "drawing_text_stb_internal.h"
