{
  "name": "catime-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "npm run build && node scripts/verify-static.js && node scripts/verify-pages.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
