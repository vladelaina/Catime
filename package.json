{
  "name": "catime-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate-download-manifest": "node scripts/generate-download-manifest.js",
    "predev": "npm run generate-download-manifest",
    "prebuild": "npm run generate-download-manifest",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test plugins/scripts/*.test.js tray/scripts/*.test.js tools/tray-icon-sorter/scripts/*.test.js",
    "verify": "npm test && npm run build && node scripts/verify-static.js && node scripts/verify-pages.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
